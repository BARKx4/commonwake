# Cognee Local Knowledge Layer

Generated by Memory Stack Cognee bootstrap.

This repo-local control plane uses the shared Cognee runtime from `E:\LLM Projects\Memory Stack` while keeping corpora, storage, reports, and logs local to this repository.

## Preset

- preset: `general`
- target repo: `C:\Users\lotti\Documents\Codex\2026-08-15\let\outputs\commonwake`

## Role Split

- `mem0-local`: short durable memory, preferences, workflows, and prior lessons
- `cognee-local.cmd`: source-backed knowledge from skills, docs, curated references, and selected repo files

## Local Files

- `cognee-local/.env`: active runtime settings
- `cognee-local/.env.example`: template copy of those settings
- `cognee-local/corpora.json`: dataset/source manifest
- `cognee-local/README.md`: local usage notes
- `cognee-local.cmd`: repo-local Cognee CLI wrapper
- `cognee-sync.cmd`: plan/ingest wrapper that reuses the shared sync script

## Common Commands

```powershell
.\cognee-local.cmd --help
.\cognee-sync.cmd plan
.\cognee-sync.cmd ingest --phase 1
.\cognee-local.cmd search "What source-backed context matters here?"
```

## Query Order

1. Query project-scoped `mem0-local`.
2. Query global `mem0-local --no-project-scope`.
3. Query `cognee-local.cmd` against the relevant dataset.
4. Inspect live repo files.
5. Browse the web only when freshness or verification matters.

## Notes

- Edit `cognee-local/corpora.json` to tune datasets for this repo.
- The shared runtime path is hard-coded in the generated wrappers so bootstrap stays one-shot and does not duplicate virtual environments.
