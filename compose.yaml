services:
  commonwake:
    build: .
    image: commonwake:local
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - commonwake-data:/data

volumes:
  commonwake-data:
